version: "2"

linters:
  default: standard
  enable:
    - misspell
    - revive
    - gocritic

linters-settings:
  revive:
    rules:
      - name: error-strings
      - name: var-naming
      - name: exported
