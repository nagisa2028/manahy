version: "2"

linters:
  default: standard
  enable:
    - misspell
    - revive
    - gocritic
    - gochecknoglobals
    - nilerr

linters-settings:
  revive:
    rules:
      - name: error-strings
      - name: var-naming
      - name: exported
